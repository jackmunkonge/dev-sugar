export type CategoryJarProps = {
  className?: string;
  color?: JarColor;
  stickerText?: string;
};

export enum JarColor {
  EMPTY,
  RED,
  ORANGE,
  YELLOW,
  GREEN,
  BLUE,
  PINK,
  BROWN,
  SILVER,
}
