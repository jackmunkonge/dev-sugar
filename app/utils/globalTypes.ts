export enum ComponentState {
  DEFAULT = "default",
  HOVER = "hover",
  FOCUS = "focus",
  ACTIVE = "active",
  LOADING = "loading",
  DISABLED = "disabled",
}

export enum ComponentSize {
  LARGE = "large",
  MEDIUM = "medium",
  SMALL = "small",
}

export enum ComponentIconPosition {
  LEFT = "left",
  RIGHT = "right",
}
